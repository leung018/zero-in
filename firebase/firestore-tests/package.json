{
  "name": "firestore-tests",
  "private": true,
  "packageManager": "yarn@4.4.1",
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.3.0",
    "firebase-tools": "^14.17.0",
    "vitest": "^3.2.4"
  },
  "scripts": {
    "test": "firebase emulators:exec --only firestore 'vitest run --root .'"
  }
}
