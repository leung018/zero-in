{
  "name": "@zero-in/firebase-manager",
  "private": true,
  "packageManager": "yarn@4.4.1",
  "dependencies": {
    "firebase": "^12.6.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase-tools": "^14.26.0",
    "vite": "^7.2.4",
    "vitest": "^4.0.13"
  },
  "scripts": {
    "build-hosting": "vite build",
    "test-firestore": "firebase emulators:exec --only firestore 'vitest run --root .'"
  }
}
